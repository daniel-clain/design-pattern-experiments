import { DataService } from "./data.service";
import IObservable from "./observable.interface";

test('', () => {
  const myConnectionObservable: IObservable = {
    onUpdate(): 
  }
  new DataService()
})